export class PageInfo {
  totalRows: number;
  totalPages: number;
  currentPage: number;
  message: string;
  pageSize:number;
  sort:object;
  data:any;
  searchData:object;
}

export const pageSize=10;
